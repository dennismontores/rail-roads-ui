import { Paper } from '@mui/material';

const Home = () => {
    return (
        <Paper sx={{ margin: '10px', padding: (theme) => theme.spacing(2) }}>
            Hello it's me
        </Paper>
    );
};

export default Home;
